/*
 * WhatsApp Cloud API — the FIRST choice: cheapest (10 XAF vs 30), threaded,
 * and the channel a Cameroonian parent actually reads.
 *
 * Deliberately still a stub. Building it needs a verified WhatsApp Business
 * number and Meta template approval, and AGENTS.md forbids adding a
 * third-party service without asking. The port and its contract suite exist
 * now, so this is one file with `runMessagingProviderContract` behind it —
 * not a redesign.
 *
 * ref: ARCHITECTURE.md section 9, docs/providers/whatsapp-cloud-api.md
 */

export {}
