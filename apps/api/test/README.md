# API tests

> TODO. Tests that MUST EXIST AND NEVER BE DELETED: cross-tenant isolation; a webhook delivered twice settles once; no reminder for an instalment paid between scheduling and sending; concurrent allocation does not over-allocate; 100000 XAF split three ways re-sums to 100000; a log fixture contains no phone number. See AGENTS.md.
