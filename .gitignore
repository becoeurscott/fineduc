node_modules/
.env
.env.*
!.env.example
dist/
.next/
.turbo/
coverage/
*.log
*.tsbuildinfo

.vercel

# Added by `vercel link`. Broad on purpose: a stray .env.production or
# .env.local must never be committable. The example file is exempted, since
# it carries no secrets and is the one env file that SHOULD be in git.
.env*
!.env.example
